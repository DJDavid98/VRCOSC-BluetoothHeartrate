<UserControl x:Class="BluetoothHeartrateModule.UI.BluetoothHeartrateRuntimeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="using:BluetoothHeartrateModule.UI"
             xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
             Width="200"
             Height="200">
    <Border>
        <Grid>
            
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="170"/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <xctk:WatermarkComboBox
                Margin="5,5,5,5"
                Grid.Row="2"
                Watermark="Devices"
                ItemsSource="{Binding Devices}"
            />
        </Grid>
    </Border>
</UserControl>
